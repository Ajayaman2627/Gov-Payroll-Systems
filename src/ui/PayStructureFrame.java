package ui;

import javax.swing.*;

public class PayStructureFrame extends JFrame {
    public PayStructureFrame() {
        setTitle("Pay Structure - Coming Soon");
        setSize(600, 400);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        add(new JLabel("Pay Structure screen coming soon...", SwingConstants.CENTER));
    }
}

package ui;

import javax.swing.*;

public class PayrollRunFrame extends JFrame {
    public PayrollRunFrame() {
        setTitle("Payroll Run - Coming Soon");
        setSize(600, 400);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        add(new JLabel("Payroll Run screen coming soon...", SwingConstants.CENTER));
    }
}
